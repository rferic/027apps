name: Mobile OTA Update

on:
  push:
    branches: [main]
    paths:
      - 'mobile/**'

concurrency:
  group: mobile-ota-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        working-directory: mobile
        run: npx tsc --noEmit

  ota:
    needs: verify
    runs-on: ubuntu-latest
    strategy:
      matrix:
        channel: [production, beta]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Publish OTA update (${{ matrix.channel }})
        working-directory: mobile
        run: eas update --channel ${{ matrix.channel }} --non-interactive
